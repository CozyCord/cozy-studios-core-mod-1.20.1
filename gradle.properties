# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx1G
org.gradle.parallel=true

# IntelliJ IDEA is not yet fully compatible with configuration cache, see: https://github.com/FabricMC/fabric-loom/issues/1349
org.gradle.configuration-cache=false

# Skip Loom's Mercury source-remap of dep sources jars. Prevents Windows
# AccessDeniedException from racing VS Code's Java LSP over zipfs handles on
# fabric-api sub-module *-sources.jar files. Trade-off: IDE nav into fabric-api
# sources shows intermediary names instead of yarn.
systemProp.fabric.loom.ci=true

# Fabric Properties
# check these on https://fabricmc.net/develop
minecraft_version=1.20.1
yarn_mappings=1.20.1+build.10
loader_version=0.17.2
loom_version=1.11-SNAPSHOT

# Mod Properties
mod_version=1.7-1.20.1
maven_group=net.cozystudios.cozystudioscore
archives_base_name=cozystudioscore

# Dependencies
fabric_version=0.92.6+1.20.1
cloth_config_version=11.1.136
mod_menu_version=7.2.2

# JEI + FTB Quests compat (compile-only)
jei_version=15.21.0.148
ftb_quests_version=2001.4.13
ftb_library_version=2001.2.9
architectury_version=9.2.14